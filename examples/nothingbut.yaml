#!/usr/bin/env web

version: 1

exec:
    command: serve
    reload: true

server:
    use: web.cli.serve.service:Marrow
    host: # bind to all interfaces
    port: 8080

application:
    use: web.core.application:Application
    root:
        index: "Hello world."
